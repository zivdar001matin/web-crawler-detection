conda_env: conda.yaml

entry_points:
    pca_train:
        command: python pca.py
    
    autoencoder_train:
        parameters:
            epochs: {type: int, default: 10}
            batch_size: {type: int, default: 512}
        command: python auto_encoder.py --epochs={epochs} --batch_size={batch_size}